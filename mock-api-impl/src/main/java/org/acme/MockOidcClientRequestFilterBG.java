package org.acme;


public class MockOidcClientRequestFilterBG{
//public class MockOidcClientRequestFilterBG implements ClientRequestFilter {

    // still receive the NamedOidcClient from the properties file
//    @Inject
//    String tokens;

    // Instead of replying with an actual token we will generate our own mock token
//    private GenerateTokenResponse accessToken;
//
//    @Override
//    public void filter(Request requestContext) throws IOException{
//        requestContext.getHeaders().add(HttpHeaders.AUTHORIZATION, "Bearer " + accessToken.getAccess() );
//    }

}
